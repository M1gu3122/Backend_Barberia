# Runtime dependencies (actually imported/used in source code)
fastapi==0.128.8
pydantic==2.12.5
sqlalchemy    # ORM and database layer
passlib bcrypt  # Password hashing
PyJWT==2.13.0  # JWT token handling
python-dotenv==1.2.1  # Environment variable loading

# Testing
pytest==8.3.1

# Optional: email templates (used by fastapi_mail in messaging/email.py)
Jinja2==3.1.6